# Backend Configuration
PORT=4000
NODE_ENV=development

# Database (SQLite by default, can be upgraded)
# DATABASE=db.sqlite

# API Base URL (for frontend in development)
REACT_APP_API_BASE=http://localhost:4000/api
